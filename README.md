# Buy Me a Coffee - Support Page

Сучасна веб-сторінка для підтримки проєкту з багатомовністю, анімаціями та адаптивним дизайном.

## 🛠 Технології

- **React 19** + **TypeScript** - UI фреймворк
- **Vite 7** - збірщик та dev сервер
- **Tailwind CSS 4** - стилізація
- **shadcn/ui** - UI компоненти на базі Radix UI
- **Framer Motion** - анімації
- **React Router** (HashRouter) - маршрутизація
- **TanStack Query** - управління станом
- **next-themes** - теми (dark/light)

## 📋 Вимоги

- **Node.js** >= 20.x
- **npm** або **bun** (проєкт підтримує обидва)

## 🚀 Розробка (Development)

### Встановлення залежностей

```bash
# Використовуючи npm
npm install

# Або використовуючи bun (якщо встановлено)
bun install
```

### Запуск dev сервера

```bash
npm run dev
```

Сервер запуститься на `http://localhost:8080` (або наступному доступному порту).

**Особливості dev режиму:**
- Hot Module Replacement (HMR) - зміни відображаються миттєво
- Source maps для дебагу
- Швидка збірка завдяки Vite
- Автоматичне відкриття браузера (залежить від налаштувань)

### Структура проєкту

```
src/
├── components/          # React компоненти
│   ├── ui/             # shadcn/ui компоненти
│   ├── HeroSection.tsx # Головна секція
│   ├── DonationOptions.tsx # Опції донату
│   └── ...
├── pages/              # Сторінки
│   ├── Index.tsx       # Головна сторінка
│   └── NotFound.tsx    # 404 сторінка
├── contexts/           # React контексти
│   └── LanguageContext.tsx # Контекст мови (uk/en)
├── hooks/              # Кастомні хуки
├── lib/                # Утиліти
├── img/                # Зображення
└── icons/              # Іконки
```

### Налаштування dev сервера

У `vite.config.ts`:
- **Порт**: 8080 (можна змінити)
- **Host**: `::` (слухає на всіх інтерфейсах)
- **Base path**: `/ruslanlap_buymeacoffe/` (для GitHub Pages)

### Лінтінг та форматування

```bash
# Перевірка коду ESLint
npm run lint

# Автоматичне форматування Prettier
npm run format
```

## 🏗 Production (Збірка та деплой)

### Локальна збірка

```bash
# Production збірка (оптимізована)
npm run build

# Development збірка (з source maps)
npm run build:dev
```

Збірка створює папку `dist/` з оптимізованими файлами:
- Мінімізований JavaScript
- Оптимізовані CSS
- Оптимізовані зображення
- Tree-shaking (видалення невикористаного коду)

### Перегляд production збірки локально

```bash
npm run preview
```

Це запускає локальний сервер для перевірки production збірки перед деплоєм.

### Деплой на GitHub Pages

#### Автоматичний деплой (через GitHub Actions)

Проєкт має налаштований GitHub Actions workflow (`.github/workflows/deploy.yml`):

1. **Автоматичний деплой** відбувається при push в гілку `master`
2. Workflow автоматично:
   - Встановлює Node.js 20
   - Встановлює залежності (`npm ci`)
   - Збирає проєкт (`npm run build`)
   - Створює SPA fallback (`dist/404.html`)
   - Деплоїть на GitHub Pages

**Щоб увімкнути автоматичний деплой:**

1. Переконайтеся, що у репозиторії увімкнено GitHub Pages:
   - Settings → Pages → Source: `GitHub Actions`

2. Push в гілку `master`:
```bash
git push origin master
```

#### Ручний деплой

```bash
# Збірка проєкту
npm run predeploy

# Деплой на GitHub Pages (через gh-pages)
npm run deploy
```

**Примітка:** Для ручного деплою потрібен пакет `gh-pages`, який вже встановлений.

### Налаштування base path

Якщо репозиторій має іншу назву, змініть `base` в `vite.config.ts`:

```typescript
export default defineConfig({
  base: "/ваша-назва-репо/", // Змініть на вашу назву
  // ...
});
```

## 🌍 Багатомовність

Проєкт підтримує дві мови:
- **Українська** (uk) - за замовчуванням
- **Англійська** (en)

Переклади знаходяться в `src/contexts/LanguageContext.tsx`.

**Додавання нової мови:**

1. Додайте мову до типу `Language`:
```typescript
export type Language = "uk" | "en" | "нова_мова";
```

2. Додайте переклади в об'єкт `translations`:
```typescript
const translations: Record<Language, Record<TranslationKey, string>> = {
  // ... існуючі мови
  нова_мова: {
    heroTitle: "Переклад",
    // ... інші ключі
  }
};
```

## 🎨 Темна/світла тема

Проєкт використовує `next-themes` для перемикання тем. Тема зберігається в `localStorage` та синхронізується з системними налаштуваннями.

## 📦 Скрипти package.json

| Скрипт | Опис |
|--------|------|
| `npm run dev` | Запуск dev сервера на порту 8080 |
| `npm run build` | Production збірка |
| `npm run build:dev` | Development збірка (з source maps) |
| `npm run preview` | Перегляд production збірки локально |
| `npm run lint` | Перевірка коду ESLint |
| `npm run format` | Форматування коду Prettier |
| `npm run predeploy` | Збірка перед деплоєм |
| `npm run deploy` | Деплой на GitHub Pages |

## 🔧 Налаштування

### Зміна порту dev сервера

У `vite.config.ts`:
```typescript
server: {
  port: 3000, // Змініть на потрібний порт
}
```

### Аліаси шляхів

Проєкт використовує аліас `@` для папки `src/`:
```typescript
import Component from "@/components/Component";
// замість
import Component from "../../components/Component";
```

## 🐛 Вирішення проблем

### Проблеми з портом 8080

Якщо порт зайнятий, Vite автоматично запропонує наступний доступний порт.

### Проблеми з деплоєм на GitHub Pages

1. Переконайтеся, що `base` в `vite.config.ts` відповідає назві репозиторію
2. Перевірте, що GitHub Pages увімкнено в налаштуваннях репозиторію
3. Переконайтеся, що workflow файл знаходиться в `.github/workflows/`

### Проблеми з залежностями

```bash
# Видаліть node_modules та lock файли
rm -rf node_modules package-lock.json

# Встановіть заново
npm install
```

## 📝 Додаткові ресурси

- [Vite Documentation](https://vitejs.dev/)
- [React Documentation](https://react.dev/)
- [Tailwind CSS Documentation](https://tailwindcss.com/)
- [shadcn/ui Documentation](https://ui.shadcn.com/)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)

## 📄 Ліцензія

Проєкт розроблений з любов'ю 💜

---

**Автор:** ruslanlap
